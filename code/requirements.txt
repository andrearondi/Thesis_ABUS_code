# Pip requirements for the abus laptop CI environment.
# These are installed inside the thesis conda env by environment.yml (via pip:).
# Versions are pinned to exact patch level for reproducibility (rules §12).
#
# Runtime dependencies (also declared in pyproject.toml with range constraints):
pynrrd==1.1.3

# Dev / CI tooling:
ruff==0.4.10
black==24.10.0
mypy==1.11.2
pytest==8.3.3
hypothesis==6.112.1
